{
  "name": "noname",
  "private": true,
  "scripts": {
    "bootstrap": "lerna run build && lerna run bootstrap",
    "build": "lerna run build",
    "start:dev": "lerna run start:dev --include-dependencies"
  },
  "dependencies": {
    "@noname/api": "file:packages/api",
    "@noname/web": "file:packages/web"
  }
}